import React from 'react';

import { Container, MaxWidth } from './styles';

const Header = () => {
  return (
    <Container>
      <MaxWidth>Logo</MaxWidth>
    </Container>
  );
};

export default Header;
